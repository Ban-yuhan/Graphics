*용어정리를 할 때 해 본것을 기준으로 용어 정리하기
참조 : 빈자리를 만드는 것
포인터 : 자리
└ 이중포인터 :
└ 포인터참조자:
	＊Call By Reference참고하기
Null : 빈자리는 있지만 없는 것
매개변수 : 전달하기 위해 사용
지역변수 :임시로 데이터를 저장하는 공간.
			동적할당된 메모리에 접근하기 위해 임시로 저장해야한다.
전역변수 : 
			시작과 끝은 전역 변수인 편이 편하다 Why?

시간 : 만약 첫 노드가 비었다면 마지막 노드를 첫 노드로 설정한다.
끝 : 임시는 끝(End)에 넣는다

컴파일러 : 변수의 이름을 붙여주는 것은 컴파일러가 담당한다. > 정확히는 코드를 짤때 우리가 이름을 붙여주고, 컴파일러는 주소값을 정해준다. ($변수명 → 주소값)
정적할당 : 변수의 이름을 붙여주고, 그 코드를 분석해 컴파일러가 변수의 주소를 할당하는것.
동적할당 : 컴파일 이후에 메모리가 할당되는 것. → 이름을 가질 수 없으므로 주소값을 식별자로 이용하고, 이 값을 저장하기 위해 포인터를 사용.

연결리스트 : 
배열 : 
큐 : 
스택 : 
배열과 연결리스트로 큐와 스택을 만들기(OR 장단점 설명) : 

